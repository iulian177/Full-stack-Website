spring.datasource.url=jdbc:postgresql://localhost:5432/employeemanager
#we encrypt these credentials usually
spring.datasource.username=postgres
spring.datasource.password=letmein
#usually, we destroy the database the moment we close the app
#create-drop in loc de update
spring.jpa.hibernate.ddl-auto=update
#we see db instruction in runtime
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#format de sql to look better
spring.jpa.properties.hibernate.format_sql=true
